import { StyleSheet } from 'react-native';

// Aqui ficam os estilos globais que podem ser usados em toda a aplicação

export const globalStyles = StyleSheet.create({
    centerContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
});